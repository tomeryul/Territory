/* =====================================================================
 * config.js — קבועי המשחק (גידול, מצלמה, גודל עולם)
 * ---------------------------------------------------------------------
 * [לוגיקה ניידת / PORTABLE LOGIC]
 * קובץ נתונים טהור. אין כאן DOM, אין Browser API.
 * ===================================================================== */

window.Territory = window.Territory || {};

(function (T) {
  'use strict';

  T.Config = {
    // העולם — מטריצה של 1000x1000. רק משבצות בבעלות/אזורים נשמרים (sparse).
    world: { width: 1000, height: 1000 },

    // נקודת ההתחלה של השחקן (מרכז העולם).
    start: { x: 500, y: 500 },

    // גידול אוטומטי איטי ומואט: ככל שהטריטוריה גדלה, משבצת חדשה לוקחת
    // יותר זמן (כמו ניהול תיק נכסים שהולך וגדל). השחקן לא בוחר — מתווסף לבד.
    growth: {
      baseMs: 3000,      // זמן בסיס למשבצת ראשונה (~3 שניות) — צמיחה מורגשת
      slowdown: 0.05,    // האטה הדרגתית עדינה ככל שהטריטוריה גדלה
      maxPerTick: 20,    // תקרת משבצות שמתווספות בטיק יחיד
    },

    // פיזור אזורים פרוצדורלי בכל העולם (דטרמיניסטי לפי seed).
    gen: {
      seed: 1337,
      step: 58,          // מרווח רשת הפיזור (אזור בערך לכל 58 משבצות)
      jitter: 22,        // הסטה אקראית של מיקום האזור
      density: 0.82,     // הסתברות להציב אזור בכל תא-רשת
      clearRadius: 16,   // רדיוס פנוי סביב ההתחלה (כדי שאפשר לגדול)
    },

    bucket: 24,          // גודל תא באינדקס המרחבי (לציור מהיר של אזורים)

    // התקדמות: ניסיון/רמות/מטבעות (החלק ה"משחקי" — RPG-ish).
    progression: {
      xpPerSec: 1,        // XP לכל שנייה פעילה
      xpPerTile: 8,       // XP לכל משבצת בטריטוריה
      xpBaseLevel: 100,   // XP לרמה הראשונה (גדל ליניארית עם הרמה)
      coinPerValue: 1000, // המרת "שווי תיק" ל"ערך נטו" (מטבעות) — להרגשת ההון
    },

    // מצלמה: scale = פיקסלים-לכל-משבצת. ניתן לזום בין "כל העולם" לקרוב לעריכה.
    camera: {
      maxScale: 56,      // זום מקסימלי (משבצת גדולה — נוח לעריכה)
      defaultScale: 24,  // זום התחלתי
      detailScale: 15,   // מעל זה — מציירים אזורים בפירוט + אנימציה
      gridScale: 7,      // מעל זה — מציירים קווי-רשת
      zoomStep: 1.35,    // מקדם זום לכל לחיצה/גלגל
    },
  };
})(window.Territory);
