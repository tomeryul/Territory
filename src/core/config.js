/* =====================================================================
 * config.js — קבועי המשחק (חוקי כלכלה, גודל עולם, viewport)
 * ---------------------------------------------------------------------
 * [לוגיקה ניידת / PORTABLE LOGIC]
 * קובץ נתונים טהור. אין כאן DOM, אין Browser API.
 * עובר 1:1 ל-React / React Native / Flutter (כקובץ constants).
 * ===================================================================== */

window.Territory = window.Territory || {};

(function (T) {
  'use strict';

  T.Config = {
    // גודל "העולם" — מטריצה ענקית. רק משבצות בבעלות נשמרות (sparse).
    world: { width: 1000, height: 1000 },

    // נקודת ההתחלה של כל שחקן (מרכז העולם).
    start: { x: 500, y: 500 },

    // החלון הנראה על המסך. העולם גדול מדי לרינדור מלא — מרנדרים חלון בלבד.
    viewport: { cols: 15, rows: 11, panStep: 1 },

    // חוקי הכלכלה: צבירת קרדיטים לפי זמן פעיל + עלויות.
    economy: {
      msPerCredit: 1000, // קרדיט אחד לכל שנייה שהאפליקציה פעילה על המסך
      claimCost: 3,      // עלות "כיבוש" משבצת ריקה צמודה לטריטוריה
    },
  };
})(window.Territory);
