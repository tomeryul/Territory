/* =====================================================================
 * config.js — קבועי המשחק (גידול, מצלמה, גודל עולם)
 * ---------------------------------------------------------------------
 * [לוגיקה ניידת / PORTABLE LOGIC]
 * קובץ נתונים טהור. אין כאן DOM, אין Browser API.
 * ===================================================================== */

window.Territory = window.Territory || {};

(function (T) {
  'use strict';

  T.Config = {
    // העולם — מטריצה של 1000x1000. רק משבצות בבעלות/אזורים נשמרים (sparse).
    world: { width: 1000, height: 1000 },

    // נקודת ההתחלה של השחקן (מרכז העולם).
    start: { x: 500, y: 500 },

    // גידול אוטומטי: משבצת נוספת לכל פרק זמן פעיל. השחקן לא בוחר —
    // המשבצת מתווספת מעצמה לטריטוריה (צמודה).
    growth: {
      msPerTile: 4000, // משבצת חדשה כל 4 שניות פעילות
      maxPerTick: 80,  // תקרת משבצות שמתווספות בטיק יחיד (הגנה מפני קפיצות)
    },

    // מצלמה: scale = פיקסלים-לכל-משבצת. ניתן לזום בין "כל העולם" לקרוב לעריכה.
    camera: {
      maxScale: 56,      // זום מקסימלי (משבצת גדולה — נוח לעריכה)
      defaultScale: 24,  // זום התחלתי
      detailScale: 15,   // מעל זה — מציירים אזורים בפירוט + אנימציה
      gridScale: 7,      // מעל זה — מציירים קווי-רשת
      zoomStep: 1.35,    // מקדם זום לכל לחיצה/גלגל
    },
  };
})(window.Territory);
